{
  "name": "airisk",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npx tsx server/src/index.ts\" \"npx vite --config client/vite.config.ts --root client\"",
    "dev:server": "npx tsx server/src/index.ts",
    "dev:client": "cd client && npx vite",
    "build": "npm run build -w server && npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
